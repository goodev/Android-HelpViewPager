package org.goodev.helpviewpager;

/**
 * The last page shown listener
 *
 */
public interface OnLastPageListener {
    /**
     * when the last page(The transparent page) is shown, will call this method
     */
    void onLastPage();
}
